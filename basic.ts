// const user = {
//   name: "shohag",
//   age: 27,
// };
// // user.location;
// // let msg = "Hello World";
// console.log(user.name);

// const greet = (person, date) => {
//   console.log(`Hello ${person}, today is ${date}`);
// };
// greet("shohag", 16);

interface User {
  name: string;
  age: number;
}

const user: User = {
  name: "shohag",
  age: 27,
};
